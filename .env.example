NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Signed httpOnly session cookie for `student_id` login
STUDENT_SESSION_SECRET=change-me

# Counselor workspace (separate route /counselor/login — not linked from student UI)
COUNSELOR_ACCESS_CODE=change-me-faculty
# Optional; falls back to STUDENT_SESSION_SECRET if unset
# COUNSELOR_SESSION_SECRET=

# Optional: where JSON data is stored (for production, point this to a writable mounted disk/volume)
# Examples:
# - Render disk: /var/data
# - Fly.io volume: /data
# - Relative: .data
# DATA_DIR=

